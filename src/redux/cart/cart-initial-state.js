import data from "../../data";
export const CartState = {
  numberCart: 0,
  Carts: [],
  _products: [],
  data,
};
/* const arr = [{ id: 1 }, { id: 2 }, { id: 1 }];
arr.filter((item, index) => {
    console.log(arr.indexOf(index)); //tim gia tri tu vi tri cua phan tu trong mang
    console.log(arr.findIndex(item.id === 1)); //tim vi tri tu gia tri cua 1 thanh phan trong mang
});
arr.splice(index, 1);
arr.indexOf(indexConLai) => item.qunati ++ */

